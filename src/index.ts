class Amount extends Number {}
class Result extends Number {}
class Rate extends Number {}

const convert = (amount: Amount, rate: Rate): Result => {
  // TODO
}

const inverseConvert = (result: Result, rate: Rate): Amount => {
  // TODO
}
